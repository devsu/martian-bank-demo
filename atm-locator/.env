# ATM Locator Service Environment Variables

# Server port (default: 8001)
PORT=8001

# MongoDB connection URL
# Use DB_URL for MongoDB Atlas or authenticated connections
DB_URL=mongodb://root:example@mongo:27017

# Alternatively, for local MongoDB without auth, use DATABASE_HOST
# DATABASE_HOST=mongo
